<div class="col-sm-12">
  <h2>Nationalities <%= link_to "add new", new_admin_nationality_url, class: "btn btn-default btn-sm" %></h2>
  
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th width="70%" class="text-center">Name</th>
        <th width="30%" class="text-center" colspan="2">Manage</th>
      </tr>
    </thead>
  
    <tbody>
      <% @nationalities.each do |nationality| %>
        <tr>
          <td><%= nationality.name %></td>
          <td class="text-center"><%= link_to 'Edit', edit_admin_nationality_path(nationality) %></td>
          <td class="text-center"><%= link_to 'Destroy', admin_nationality_path(nationality), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>