<p id="notice"><%= notice %></p>

<h1>Listing Settings</h1>

<table>
  <thead>
    <tr>
      <th>Google analytics</th>
      <th>Bing verification</th>
      <th>Alexa verification</th>
      <th>Google verification</th>
      <th>Yandex verification</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @settings.each do |setting| %>
      <tr>
        <td><%= setting.google_analytics %></td>
        <td><%= setting.bing_verification %></td>
        <td><%= setting.alexa_verification %></td>
        <td><%= setting.google_verification %></td>
        <td><%= setting.yandex_verification %></td>
        <td><%= link_to 'Show', setting %></td>
        <td><%= link_to 'Edit', edit_setting_path(setting) %></td>
        <td><%= link_to 'Destroy', setting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Setting', new_setting_path %>
