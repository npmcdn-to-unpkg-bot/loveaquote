<% content_for :content %>
<div class="col-sm-12">
    <h2>Edit <%= link_to "book", admin_book_url(@book), class: "btn btn-default btn-sm" %> <%= link_to "Delete", admin_book_url(@book), method: :delete, class: "btn btn-danger btn-sm" %></h2>
    <%= render 'form' %>
</div>
<% end %>