<div class="col-sm-12">
  <ol class="breadcrumb bc-3"> 
    <li><%= link_to "Home", admin_root_url %></li>
    <li><%= link_to "Authors", admin_authors_url %></li> 
    <li class="active"><%= @author.name %></li>
  </ol>
  
  <div class="row">
    <div class="col-sm-2">
      <% if @author.image.present? %>
        <%= image_tag @author.image_url(:small), class: "thumbnail", style: "margin-bottom: 0px" %>
      <% else %>
        <%= image_tag "http://placehold.it/150x150", class: "thumbnail" %>
      <% end %>
    </div>
    <div class="col-sm-7">
      <h2 class="text-right"><%= @author.name %> <%= link_to "view", author_url(@author), class: "btn btn-default btn-sm", target: "_blank" %> <%= link_to "edit", edit_admin_author_url(@author), class: "btn btn-default btn-sm" %> <%= link_to "new quote", "", class: "btn btn-default btn-sm add_new_quote" %></h2>
    </div>
  </div>

  <div class="row">
    <div class="col-sm-9">
      <div class="panel minimal minimal-gray">
        <div class="panel-heading">
          <div class="panel-title">
            
          </div>
          <div class="panel-options">
            <ul class="nav nav-tabs">
              <li class="active"><a href="#quotes" data-toggle="tab">Self Quotes</a></li>
              <li><a href="#book-quotes" data-toggle="tab">Book Quotes</a></li>
              <li><a href="#quote-topic-suggestions" data-toggle="tab">Topic Suggestions</a></li>
            </ul>
          </div>
        </div>
        
        <div class="panel-body">
          
          <div class="tab-content">
            
            <div class="tab-pane active" id="quotes">
              <table class="table table-striped table-bordered table-hover">
                <thead>
                  <th width="90%" class="text-center">Quote</th>
                  <th width="10%" class="text-center" colspan="2">Manage</th>
                </thead>
                
                <tbody>
                  <% @author.quotes.order(text: :asc).each do |quote| %>  
                  <tr>
                    <td><%= quote.text %></td>
                    <td class="text-center"><%= link_to "Edit", "#", class: "edit-quote btn btn-default btn-xs", data: { url: "/admin/quotes/#{quote.id}" } %></td>
                    <td class="text-center"><%= link_to "Delete", admin_quote_url(quote), method: :delete, remote: true, class: "delete-quote btn btn-red btn-xs" %></td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
            
            <div class="tab-pane active" id="book-quotes">
              <table class="table table-striped table-bordered table-hover">
                <thead>
                  <th width="20%" class="text-center">Book</th>
                  <th width="70%" class="text-center">Quote</th>
                  <th width="10%" class="text-center" colspan="2">Manage</th>
                </thead>
                
                <tbody>
                  <% @author.book_quotes.order(text: :asc).each do |quote| %>  
                  <tr>
                    <% book = Book.find(quote.source_id) %>
                    <td class="text-center"><%= link_to book.name, admin_book_url(book) %></td>
                    <td><%= quote.text %></td>
                    <td class="text-center"><%= link_to "Edit", "#", class: "edit-quote btn btn-default btn-xs", data: { url: "/admin/quotes/#{quote.id}" } %></td>
                    <td class="text-center"><%= link_to "Delete", admin_quote_url(quote), method: :delete, remote: true, class: "delete-quote btn btn-red btn-xs" %></td>
                  </tr>
                  <% end %>
                </tbody>
              </table>
            </div>            
            
            <div class="tab-pane" id="quote-topic-suggestions">
              <div class="row">
                <table class="table table-striped table-bordered table-hover">
                  <thead>
                    <tr>
                      <th width="60%" class="text-center">Quote</th>
                      <th width="20%" class="text-center">Topic</th>
                      <th width="20%" class="text-center" colspan="2">Manage</th>
                    </tr>
                  </thead>
                
                  <tbody>
                    <% @author.quote_topic_suggestions.where(read: false).each do |quote_topic_suggestion| %>
                      <tr>
                        <td><%= quote_topic_suggestion.quote.text %></td>
                        <td class="text-center"><%= quote_topic_suggestion.topic.name %></td>
                        <td class="text-center"><%= link_to "Accept", accept_admin_quote_topic_suggestion_url(id: quote_topic_suggestion.id), class:"accept_suggestion", remote: true %></td>
                        <td class="text-center"><%= link_to "Decline", decline_admin_quote_topic_suggestion_url(id: quote_topic_suggestion.id), class:"decline_suggestion", remote: true %></td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
              </div>          
            </div>
            
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="modal fade in" id="new-quote-modal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h4 class="modal-title">Edit Quote</h4>
      </div>
      <div class="modal-body">
        <%= simple_form_for([:admin, Quote.new]) do |f| %>
          <div class="form-inputs">          
            <%= f.input :text %>
            <%= f.hidden_field :author_id, value: @author.id %>
          </div>
          <div class="form-actions">
            <%= f.button :submit %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div class="modal fade in" id="edit-quote-modal">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h4 class="modal-title">Edit Quote</h4>
      </div>
      <div class="modal-body">
        <%= simple_form_for([:admin, Quote.first], remote: true, html: {"data-type": "json"}) do |f| %>
          <div class="form-inputs">          
            <%= f.input :text %>
          </div>
          <div class="form-actions">
            <%= f.button :submit %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>

<script type="text/javascript">
  $(function() {
    var edit_quote;
    
    $(".accept_suggestion")
    .on("ajax:complete", function(e, data, status, xhr) {
        $(this).closest("tr").remove();
    });
  
    $(".decline_suggestion")
    .on("ajax:complete", function(e, data, status, xhr) {
        $(this).closest("tr").remove();
    });
    
    $(".add_new_quote").on("click", function(e) {
      e.preventDefault();
      $("#new-quote-modal").modal('show');
    });
    
    $(".delete-quote").on("ajax:complete", function(xhr, status) {
      $(this).closest("tr").remove();
    });
    
    $(".delete_featured_topic").on("ajax:complete", function(xhr, status) {
      $(this).closest("tr").remove();
    });
    
    $(".edit-quote").on("click", function(e) {
      e.preventDefault();
      $(".simple_form.edit_quote").attr("action", $(this).data("url"));
      $(".edit_quote #quote_text").val($(this).parent().prev().text());
      $("#edit-quote-modal").modal('show');
      edit_quote = $(this);
    });
    
    $(".simple_form.edit_quote")
    .on("ajax:complete", function(e, xhr, status, error) {
      $("#edit-quote-modal").modal('hide');
      edit_quote.parent().prev().prev().text((xhr.responseJSON.text));
    });
  });
</script>