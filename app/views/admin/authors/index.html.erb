<div class="col-sm-12">

  <h2>Authors <%= link_to "add new", new_admin_author_url, class: "btn btn-default btn-sm" %></h2>
  
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th width="10%" class="text-center">Image</th>
        <th width="60%" class="text-center">Name</th>
        <th width="30%" class="text-center" colspan="3">Manage</th>
      </tr>
    </thead>
  
    <tbody>
      <% @authors.each do |author| %>
        <tr>
          <td class="text-center"><%= author.image.present? ? (image_tag author.image_url(:tiny)) : (image_tag "http://placehold.it/50x50") %></td>
          <td><%= author.name %></td>
          <td class="text-center"><%= link_to 'Show', admin_author_url(author), class: "btn btn-primary btn-xs" %></td>
          <td class="text-center"><%= link_to 'Edit', edit_admin_author_url(author), class: "btn btn-blue btn-xs" %></td>
          <td class="text-center"><%= link_to 'Destroy', admin_author_url(author) , method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-red btn-xs" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  
  <div class="text-center">
    <%= paginate @authors %>
  </div>
</div>