<% content_for :content do %>
<div class="col-sm-12">
    <h2>Edit <%= link_to "topic", admin_topic_url(@topic), class: "btn btn-default btn-sm" %> <%= link_to "Delete", admin_topic_url(@topic), method: :delete, class: "btn btn-danger btn-sm" %></h2>
    <%= render 'form' %>
</div>
<% end %>