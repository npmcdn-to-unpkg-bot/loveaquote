<% content_for :title, "Tv Shows by Alphabet #{@alphabet}" %>
<% content_for :canonical, "#{alphabet_tv_shows_url(@alphabet, format: :html)}" %>

<% content_for :content do %>
  <div class="col-xs-12">
    <h1 class="main-title text-center"><%= "Tv Shows by Alphabet #{@alphabet}" %></h1>
    <div class="row">
      <% @tv_shows.each do |tv_show| %>
        <div class="col-xs-12 col-sm-12 col-md-6 col-lg-4">
          <%= link_to tv_show.name + " Quotes", tv_show_url(tv_show, format: :html), title: tv_show.name + " Quotes", class: "link-box text-center" %>
        </div>
      <% end %>
    </div>
  </div>
<% end %>

<% content_for :additional_tags do %>
  <meta name="robots" content="noindex,follow">
<% end %>
