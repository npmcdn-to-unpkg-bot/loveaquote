<% content_for :title, @title %>

<% content_for :canonical, @canonical %>

<% content_for :heading, "Topics by Alphabet #{@alphabet}" %>

<% content_for :content do %>

  <% first = @topics.count / 3 + @topics.count % 3 %>
  <% second = first + @topics.count / 3 %>
  <% third =  second + @topics.count / 3 %>
  <div class="row">
    <div class="col-md-4">
      <ul>
        <% ( 1..first ).each do |count| %>
          <li><%= link_to @topics[count-1].name, topic_url(@topics[count-1]) %></li>
        <% end %>
      </ul>
    </div>
    <div class="col-md-4">
      <ul>
        <% ( ( first + 1 )..second ).each do |count| %>
          <li><%= link_to @topics[count-1].name, topic_url(@topics[count-1]) %></li>
        <% end %>
      </ul>
    </div>
    <div class="col-md-4">
      <ul>
        <% ( ( second+1 )..third ).each do |count| %>
          <li><%= link_to @topics[count-1].name, topic_url(@topics[count-1]) %></li>
        <% end %>
      </ul>
    </div>
  </div>

<% end %>