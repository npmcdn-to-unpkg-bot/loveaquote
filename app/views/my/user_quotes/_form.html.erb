<%= simple_form_for([:my, @user_quote], remote: true, html: {class: "form-inline text-center", id: "user-add-quote"}) do |f| %>
    <%= f.hidden_field :quote_id %>
    <%= f.association :user_lists, label: false %>
    <%= f.submit "Save Quote", class: "btn btn-blue" %>
<% end %>